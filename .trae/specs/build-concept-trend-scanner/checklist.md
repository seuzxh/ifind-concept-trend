# Checklist

## 接口验证 — Task 0

### 0.1 Token 鉴权
- [ ] get_access_token 接口连通，返回有效 access_token
- [ ] 确认 access_token 有效期和 IP 绑定限制
- [ ] 确认 refresh_token 可正常使用

### 0.2 概念人气明细 (p03797)
- [ ] data_pool(p03797) 接口连通，返回数据
- [ ] 确认 `p03797_f001` 字段含义（预期：概念板块名称）
- [ ] 确认 `p03797_f002` 字段为交易日期，格式正确
- [ ] 确认 `p03797_f009` 字段为自选热度，数据格式正确
- [ ] 确认 `p03797_f010` 字段为自选热度变化率，数据格式正确

### 0.3 板块热门成分股 (p03798)
- [ ] data_pool(p03798) 接口连通，返回数据
- [ ] 确认 `jydm` 字段为交易代码，格式如 `300033.SZ`
- [ ] 确认 `jydm_mc` 字段为交易代码名称
- [ ] 确认 `p03798_f001` 字段为交易日期
- [ ] 确认 `p03798_f012` 字段为涨跌幅（%），数据格式正确
- [ ] 确认 `p03798_f016` 字段为区间开始日期

### 0.4 历史日K线 (cmd_history_quotation)
- [ ] 接口连通，返回近 5 个交易日日线数据
- [ ] `open/high/low/close` 价格字段完整
- [ ] `volume/amount` 成交量/额字段完整
- [ ] 取最后一根 K 线的 close 作为昨收价，数值合理
- [ ] 验证 startdate/enddate 参数格式

### 0.5 开盘 5 分钟 K 线 (high_frequency)
- [ ] 接口连通，返回 09:30~09:35 的 5 根 1 分钟 K 线
- [ ] `open/high/low/close` 价格字段完整
- [ ] `volume/amount` 成交量/额字段完整
- [ ] `changeRatio` 涨跌幅字段完整
- [ ] `LB`（量比）技术指标字段存在且数值合理

### 0.6 交易日判断 (get_trade_dates)
- [ ] 接口连通，正确返回交易日列表
- [ ] 交易日返回日期，非交易日返回空

### 0.7 验证结果整理
- [ ] 每个接口的返回 JSON 格式已记录
- [ ] 更新 spec.md 中的字段说明

## 核心功能
- [ ] SQLite 数据库表结构创建正确（含 kline_daily 表），数据可正常读写
- [ ] 每日数据同步（人气数据含非交易日）功能正常
- [ ] 日K线数据写入 kline_daily 表，昨收价可正确查询
- [ ] 涨幅计算逻辑正确（当前价 vs 日K昨收价）
- [ ] 实体涨幅（5min K 线 open→close）计算逻辑正确
- [ ] 成交额（5min K 线 amount 累加）计算逻辑正确
- [ ] 量比（LB 指标）数据获取正确
- [ ] 多因子加权综合评分公式实现正确
- [ ] 概念板块评分（基于成份股聚合）逻辑正确
- [ ] 评分阈值可通过配置文件调整

## 推送与调度
- [ ] 企业微信 webhook 推送成功
- [ ] 推送报告内容清晰、格式正确（Top 5 板块 + Top 10 个股）
- [ ] 推送失败时不阻塞主流程，日志记录正常
- [ ] 定时调度在交易日 9:36 正确触发盘中扫描
- [ ] 人气数据每日同步（含非交易日）正常执行
- [ ] 非交易日跳过盘中扫描（行情数据采集）
- [ ] 手动触发扫描正常工作
- [ ] 指定日期回溯扫描正常工作

## CLI 入口
- [ ] `python -m scanner scan` 命令正常执行
- [ ] `python -m scanner serve` 命令正常启动定时服务
- [ ] `python -m scanner sync` 命令正常执行数据同步

## 代码质量
- [ ] 所有代码符合 Google Python 编码规范
- [ ] 类型注解覆盖公开 API
- [ ] 关键模块有单元测试覆盖
